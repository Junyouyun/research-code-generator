# 任务 002：PDF 解析

## 用户要求

在数据库和模型完成后，继续实现论文 PDF 解析。

## 本次目标

- 使用 PyMuPDF 读取用户上传的 PDF。
- 按页提取正文文本。
- 输出基础结构化结果，包括标题、摘要、页数、全文和页面文本。
- 保持第一版实现简单，不做复杂版面分析。
- 修正流水线状态文案，保证解析流程可以正常运行。

## 当前进度

- 状态：已完成
- 已完成：
  - 创建任务记录文件。
  - 实现 `paper_parser.py` 的真实 PDF 文本解析。
  - 输出 `title`、`abstract`、`page_count`、`pages`、`sections`、`full_text` 等字段。
  - 修正 `pipeline.py` 中的状态文案。
  - 调整 PyMuPDF 为解析时加载，避免依赖未安装时影响后端启动。
- 待完成：
  - 安装后端依赖后，用真实 PDF 做端到端解析验证。
  - 后续增强章节识别。
  - 后续增强表格、图片、公式抽取。

-后续补充
  -某一步失败后自动重试或修复。
  -报告生成和代码生成并行。
  -多 Agent 协作。
  -人工确认中间结果。
  -长任务断点续跑。
  -状态持久化和恢复。
