# 任务 005：代码项目规划

## 用户要求

在报告生成完成后，实现 `code_planner.py`，让系统根据论文分析结果规划代码项目结构。

## 本次目标

- 基于 `analysis` 输出代码项目规划。
- 规划内容包括语言、文件列表、运行命令、依赖、入口文件和模块职责。
- 保持第一版简单，不引入复杂模板系统。
- 修复代码生成链路中的乱码残留，保证后端语法可通过。

## 当前进度

- 状态：已完成
- 已完成：
  - 创建任务记录文件。
  - 完成 `code_planner.py` 规则版规划逻辑。
  - 规划默认包含 `README.md`、`main.py`、`requirements.txt`。
  - 根据论文分析结果补充 `data.py`、`model.py`、`train.py`、`evaluate.py` 等模块。
  - 修复 `pipeline.py` 和 `code_runner.py` 中的乱码残留。
  - 验证规划函数可以正常输出。
- 待完成：
  - 后续实现 `code_generator.py`，按规划生成对应文件。
  - 后续接入 LLM，让规划更贴近论文方法。
